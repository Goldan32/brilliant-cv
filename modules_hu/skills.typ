#import "../brilliant-CV/template.typ": *

#cvSection("Készségek")

#cvSkill(
  type: [Nyelvek],
  info: [Angol (C1) #hBar() Magyar (anyanyelv)]
)

#cvSkill(
  type: [Programozás],
  info: [C #hBar() C++ #hBar() Rust #hBar() Python #hBar() BASH #hBar() Robot Framework #hBar() Matlab #hBar() Verilog]
)

#cvSkill(
  type: [Fejlesztés],
  info: [GCC/G++ #hBar() Makefile #hBar() CMake #hBar() Visual Studio #hBar() VSCode #hBar() Linux kernel #hBar() OpenBMC #hBar() Petalinux]
)

#cvSkill(
  type: [Egyéb],
  info: [Git #hBar() Linux felhasználói ismeretek #hBar() Yocto Project #hBar() KiCAD #hBar() JIRA #hBar() Confluence]
)
